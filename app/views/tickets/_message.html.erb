<div class="card mb-3">
  <div class="card-body">
    <p><%= message['content'] %></p>
    <small class="text-muted">
      By: <%= message.dig('user', 'email') || "Unknown" %> 
      (<%= message.dig('user', 'role') || "User" %>)
    </small>
  </div>
</div>